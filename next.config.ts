import type { NextConfig } from "next";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { env } from "@/env"; // Allows env schema validation on build

const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
